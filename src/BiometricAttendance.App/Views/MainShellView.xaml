<Window x:Class="BiometricAttendance.App.Views.MainShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:BiometricAttendance.App.Views"
        Title="Biometric Attendance System"
        MinWidth="1100" MinHeight="660"
        Width="1280" Height="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="{StaticResource FontInter}">

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Sidebar -->
            <ColumnDefinition Width="{StaticResource SidebarWidth}"/>
            <!-- Content -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ══════════════════════════════════
             SIDEBAR
             ══════════════════════════════════ -->
        <Border Grid.Column="0"
                Background="{StaticResource SidebarBrush}">
            <views:SidebarView DataContext="{Binding Sidebar}"/>
        </Border>

        <!-- ══════════════════════════════════
             RIGHT PANEL: TopBar + Page Content
             ══════════════════════════════════ -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="{StaticResource TopBarHeight}"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TopBar -->
            <Border Grid.Row="0"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <views:TopBarView DataContext="{Binding TopBar}"/>
            </Border>

            <!-- Page Content — DataTemplates in App.xaml map VM → View -->
            <ContentControl Grid.Row="1"
                            Content="{Binding CurrentPage}"
                            Background="{StaticResource BackgroundBrush}"/>
        </Grid>
    </Grid>
</Window>
