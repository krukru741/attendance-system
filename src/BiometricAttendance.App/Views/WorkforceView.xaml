<UserControl x:Class="BiometricAttendance.App.Views.WorkforceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BiometricAttendance.App.Views"
             xmlns:vm="clr-namespace:BiometricAttendance.App.ViewModels"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             d:DataContext="{d:DesignInstance Type=vm:WorkforceViewModel}">

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding InitializeCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="30,20" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel>
                    <TextBlock Text="Workforce" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}" />
                    <TextBlock Text="Manage employees, departments, and positions." FontSize="14" Foreground="{DynamicResource MutedTextColor}" Margin="0,4,0,0" />
                </StackPanel>

                <Button Grid.Column="1" 
                        Content="+ Add Employee" 
                        Command="{Binding AddEmployeeCommand}"
                        Visibility="{Binding CanAddEmployee, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Padding="16,8"
                        Background="{DynamicResource PrimaryColor}"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="Medium"
                        Cursor="Hand" />
            </Grid>
        </Border>

        <!-- Filters -->
        <Border Grid.Row="1" Padding="30,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="150" />
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <TextBox Grid.Column="0" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Padding="10,8" 
                         FontSize="14"
                         BorderBrush="{DynamicResource BorderColor}"
                         Foreground="{DynamicResource TextColor}">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4" Background="White">
                                            <Grid>
                                                <TextBlock Text="Search by name or code..." Foreground="{DynamicResource MutedTextColor}" Margin="10,0,0,0" VerticalAlignment="Center" Panel.ZIndex="-1" IsHitTestVisible="False">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Department Filter -->
                <ComboBox Grid.Column="2" 
                          ItemsSource="{Binding Departments}" 
                          SelectedItem="{Binding SelectedDepartment}"
                          DisplayMemberPath="Name"
                          Padding="10,8"
                          FontSize="14" />

                <!-- Status Filter -->
                <ComboBox Grid.Column="4" 
                          ItemsSource="{Binding StatusOptions}" 
                          SelectedItem="{Binding SelectedStatus}"
                          Padding="10,8"
                          FontSize="14" />
            </Grid>
        </Border>

        <!-- Employee List -->
        <Border Grid.Row="2" Padding="30,0,30,30">
            <DataGrid ItemsSource="{Binding Employees}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{DynamicResource BorderColor}"
                      BorderThickness="1"
                      BorderBrush="{DynamicResource BorderColor}"
                      Background="White"
                      RowBackground="White"
                      AlternatingRowBackground="#FAFAFA"
                      RowHeight="50">
                
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F9FA"/>
                        <Setter Property="Foreground" Value="{DynamicResource MutedTextColor}"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                        <Setter Property="Padding" Value="16,12"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="16,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#EBF5FF"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Code" Binding="{Binding EmployeeCode}" Width="100" />
                    <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*" />
                    <DataGridTextColumn Header="Department" Binding="{Binding Department.Name}" Width="150" />
                    <DataGridTextColumn Header="Position" Binding="{Binding Position.Name}" Width="150" />
                    
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="8,4" CornerRadius="12" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding EmploymentStatus}" Value="Active">
                                                    <Setter Property="Background" Value="#E6F4EA"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding EmploymentStatus}" Value="Inactive">
                                                    <Setter Property="Background" Value="#FEF7E0"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding EmploymentStatus}" Value="Archived">
                                                    <Setter Property="Background" Value="#F1F3F4"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding EmploymentStatus}" FontSize="12" FontWeight="Medium">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding EmploymentStatus}" Value="Active">
                                                        <Setter Property="Foreground" Value="#1E8E3E"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EmploymentStatus}" Value="Inactive">
                                                        <Setter Property="Foreground" Value="#F29900"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EmploymentStatus}" Value="Archived">
                                                        <Setter Property="Foreground" Value="#5F6368"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Actions" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button Content="View" 
                                            Command="{Binding DataContext.ViewProfileCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Visibility="{Binding DataContext.CanViewEmployee, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Background="Transparent" Foreground="{DynamicResource PrimaryColor}" BorderThickness="0" Cursor="Hand" />
                                    <Button Content="Edit" 
                                            Command="{Binding DataContext.EditEmployeeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Visibility="{Binding DataContext.CanEditEmployee, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Background="Transparent" Foreground="{DynamicResource PrimaryColor}" BorderThickness="0" Cursor="Hand" />
                                    <Button Content="Archive" 
                                            Command="{Binding DataContext.ArchiveEmployeeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Visibility="{Binding DataContext.CanArchiveEmployee, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Background="Transparent" Foreground="Red" BorderThickness="0" Cursor="Hand" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
