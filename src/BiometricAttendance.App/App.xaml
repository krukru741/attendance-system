<Application x:Class="BiometricAttendance.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:BiometricAttendance.App.Converters"
             xmlns:vm="clr-namespace:BiometricAttendance.App.ViewModels"
             xmlns:views="clr-namespace:BiometricAttendance.App.Views"
             ShutdownMode="OnLastWindowClose">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Colors.xaml"/>
                <ResourceDictionary Source="Resources/Brushes.xaml"/>
                <ResourceDictionary Source="Resources/Typography.xaml"/>
                <ResourceDictionary Source="Resources/Dimensions.xaml"/>
                <ResourceDictionary Source="Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- ── Converters ─────────────────── -->
            <conv:BoolToVisibilityConverter  x:Key="BoolToVisibility"/>
            <conv:InverseBoolConverter       x:Key="InverseBool"/>
            <conv:BoolToTextConverter        x:Key="LoadingToText"/>
            <conv:BoolToEyeIconConverter     x:Key="BoolToEyeIcon"/>
            <conv:InitialsConverter          x:Key="InitialsConverter"/>
            <conv:AttendanceStatusToBrushConverter x:Key="AttendanceStatusToBrush"/>

            <!-- ── DataTemplates (ViewModel → View) ─────────── -->
            <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
                <views:DashboardView/>
            </DataTemplate>

        </ResourceDictionary>
    </Application.Resources>
</Application>
